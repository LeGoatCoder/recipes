# Import necessary libraries
!pip install git+https://github.com/openai/whisper.git
import whisper

# Load the Whisper model
# There are several models available with varying sizes
model = whisper.load_model("large-v2").to("cuda:0")

# Transcribe an audio file
start = time.time()
result = model.transcribe("20.mp3")
print(f"Transcribed in {time.time() - start} seconds.")

# Save the transcription to a text file
f = open("20-text-dump.txt", "w+")
f.write(result["text"])
f.close()

# Note: Some manual effort is required to correct certain inaccuracies in the transcription
